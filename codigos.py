#procura string dentro do código html da página
'''i = (driver.page_source).find(dados.ofsc)
    OFSC = ""
    while driver.page_source[i] != "'":
        OFSC += driver.page_source[i]
        i += 1
        auth = driver.find_element_by_name('AuthState').get_attribute("value")'''